import 'package:flutter/material.dart';

class Dimensions {
  static final borderRadius = BorderRadius.circular(14);
  static final marginHorizontal = EdgeInsets.symmetric(horizontal: 20);
  static final marginScreen = EdgeInsets.all(30);
  static final space = 20.0;
  static final radiusScreen = BorderRadius.only(
    topLeft: Radius.circular(20),
    topRight: Radius.circular(20),
  );
}
